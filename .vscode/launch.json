{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run FreeGLUT App",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "preLaunchTask": "build",
            "externalConsole": true
        }
    ]
}
